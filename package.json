{
  "name": "relativity",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "node server.js",
    "build": "rm -rf dist && parcel build && cp -R json dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.0",
    "parcel": "^2.6.0"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "d3": "^7.4.4",
    "express": "^4.18.1"
  }
}
